apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana
  namespace: monitoring
spec:
  routes:
    - match: Host(`grafana.${TLD}`)
      priority: 10
      services:
        - name: grafana-service
          port: 3000