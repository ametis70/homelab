apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: immich-ingress
  namespace: immich
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`immich.${DOMAIN}`)"
      priority: 10
      services:
        - name: immich-server
          port: 2283
