apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: outline-files
  namespace: outline
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: longhorn

