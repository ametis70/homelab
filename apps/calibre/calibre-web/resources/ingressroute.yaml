apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: calibre-web
  namespace: calibre
spec:
  routes:
    - match: Host(`calibre-web.${DOMAIN}`)
      priority: 10
      services:
        - name: calibre-web
          port: 8083

