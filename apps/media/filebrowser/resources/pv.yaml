apiVersion: v1
kind: PersistentVolume
metadata:
  name: filebrowser-config
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  csi:
    driver: driver.longhorn.io
    volumeHandle: filebrowser-config
  claimRef:
    namespace: media
    name: filebrowser-config
