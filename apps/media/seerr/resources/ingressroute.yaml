apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: seerr
  namespace: media
spec:
  routes:
    - match: Host(`seerr.${DOMAIN}`)
      priority: 10
      services:
        - name: seerr
          port: 5055
