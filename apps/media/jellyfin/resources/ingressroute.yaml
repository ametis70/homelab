apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: jellyfin
  namespace: media
spec:
  routes:
    - match: Host(`jellyfin.${DOMAIN}`)
      priority: 10
      services:
        - name: jellyfin
          port: 8096

