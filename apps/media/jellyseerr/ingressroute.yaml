apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: jellyseerr
  namespace: media
spec:
  routes:
    - match: Host(`jellyseerr.${TLD}`)
      priority: 10
      services:
        - name: jellyseerr
          port: 5055
