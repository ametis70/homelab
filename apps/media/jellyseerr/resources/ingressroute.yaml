apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: jellyseerr
  namespace: media
spec:
  routes:
    - match: Host(`jellyseerr.${DOMAIN}`)
      priority: 10
      services:
        - name: jellyseerr
          port: 5055
