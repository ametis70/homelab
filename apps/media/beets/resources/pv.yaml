apiVersion: v1
kind: PersistentVolume
metadata:
  name: beets-config
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  csi:
    driver: driver.longhorn.io
    volumeHandle: beets-config
  claimRef:
    namespace: media
    name: beets-config
