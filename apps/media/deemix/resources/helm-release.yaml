apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  namespace: media
  name: deemix
spec:
  interval: 24h0m0s
  chart:
    spec:
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 4.2.0
  values:
    controllers:
      deemix:
        containers:
          app:
            image:
              repository: ghcr.io/bambanah/deemix
              tag: v4.5.0@sha256:52a74c384c9b771d7cd2d05b9f122c39c69aaa241d92dcc33b96e7d54c342f22
            env:
              TZ: "America/Argentina/Buenos_Aires"
              PUID: 568
              PGID: 568
              DEEMIX_MUSIC_DIR: "/downloads"
              DISABLE_OWNERSHIP_CHECK: true
            probes:
              liveness: &probes
                enabled: false
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: &port 6595
                  initialDelaySeconds: 0
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness: *probes
    service:
      app:
        controller: deemix
        ports:
          http:
            port: *port
    persistence:
      config:
        existingClaim: deemix-config
        globalMounts:
          - path: /config
      downloads:
        existingClaim: media-downloads-pvc
        globalMounts:
          - path: /downloads/deemix
