apiVersion: v1
kind: PersistentVolume
metadata:
  name: huntarr-config
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  csi:
    driver: driver.longhorn.io
    volumeHandle: huntarr-config
  claimRef:
    namespace: media
    name: huntarr-config
