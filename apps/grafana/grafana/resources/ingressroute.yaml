apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana
  namespace: grafana
spec:
  routes:
    - match: Host(`grafana.${DOMAIN}`)
      priority: 10
      services:
        - name: grafana-service
          port: 3000