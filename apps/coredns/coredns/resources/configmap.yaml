---
apiVersion: v1
kind: ConfigMap
metadata:
  name: coredns-custom
  namespace: kube-system
data:
  ${DOMAIN}.override: |
    template IN A ${DOMAIN} {
        match "^(.+\.)?${DOMAIN}\.$"
        answer "{{ .Name }} 60 IN A 10.0.30.100"
        fallthrough
    }
