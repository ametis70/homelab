apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: n8n-ingress
  namespace: n8n
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`n8n.${DOMAIN}`)"
      priority: 10
      services:
        - name: n8n
          port: 5678
