---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: vaultwarden
spec:
  interval: 24h
  retryInterval: 1m
  timeout: 5m
  path: ./apps/vaultwarden/vaultwarden/resources
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: vaultwarden
  dependsOn:
    - name: cluster-secrets
      namespace: flux-system
    - name: traefik
      namespace: traefik
  postBuild:
    substituteFrom:
      - kind: Secret
        name: vaultwarden-secrets-system
        optional: false
      - kind: Secret
        name: ingress-secrets-system
        optional: false