---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-cloudflare-ddns
  namespace: flux-system
spec:
  interval: 24h
  retryInterval: 1m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./infrastructure/cloudflare-ddns
  prune: false
  wait: true
  dependsOn:
    - name: cluster-secrets
  postBuild:
    substituteFrom:
      - kind: Secret
        name: cloudflare-ddns-secrets-system
        optional: false
