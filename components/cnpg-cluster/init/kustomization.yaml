---
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

components:
  - ../base

patches:
  - path: cluster-backup-patch.yaml
    target:
      kind: Cluster
  - path: cluster-no-restore-patch.yaml
    target:
      kind: Cluster

replacements:
  - source:
      kind: Cluster
      fieldPath: metadata.namespace
    targets:
      - select:
          kind: Cluster
        fieldPaths:
          - spec.plugins.0.parameters.serverName
        options:
          delimiter: "/"
          index: 0
  - source:
      kind: Cluster
      fieldPath: metadata.name
    targets:
      - select:
          kind: Cluster
        fieldPaths:
          - spec.plugins.0.parameters.serverName
        options:
          delimiter: "/"
          index: 1
